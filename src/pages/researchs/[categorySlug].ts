import React from 'react'

import ContentList from '@/components/ContentList'

import type { ContentListProps } from '@/components/ContentList/types'

function CategoryPage({
  groupedContent
}: ContentListProps) {
  
}

export default CategoryPage;